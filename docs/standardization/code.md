# Padronização de Código (Singularity)

- Arquivos principais: `src/server.ts`, `src/protocol.ts`, `src/agents/*`.
- Usar constantes `Types` e tipos explícitos.
- Tratamento de erros com fallback; sem segredos em logs.

