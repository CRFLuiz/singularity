# Padronização de Nomenclatura (Singularity)

- Tipos de mensagem: `chat.message`, `ack`, `chat.reply`, `models.list`, `ping`, `pong`, `error`.
- Env: `SINGULARITY_PORT`, `OPENROUTER_API_KEY`, `OPENROUTER_BASE_URL`.
- Model IDs: nomes conforme docs dos agentes.

